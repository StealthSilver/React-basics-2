React props 

Props are the information that you pass to a JSX tag 
<Product title= "phone" price = "30k>

we can also assign default values in props 
{title, price =1}

passing arrays to props

Rendering arrays 

instead of individual vlue we can send array of html elements

Conditionals in React

Adding elements on the basis of some conditions 

{price >= 1000 ? <p> Discount : 5% </p> : null}